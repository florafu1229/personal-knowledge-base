# AI / RAG / LangChain / DeepEval 面试问题与基础知识

这份文档用于准备 AI 测试、RAG 应用、LLM 评测、Agent 工程实践相关面试。

重点覆盖:

- LLM 基础概念。
- Prompt Engineering。
- Embedding 和向量数据库。
- RAG 基础知识。
- LangChain 基础知识。
- AI Agent 基础知识。
- DeepEval 和 LLM 应用评测。
- RAG 评测指标。
- 面试常问问题与回答要点。

---

# 一、LLM 基础知识

## 1. 什么是 LLM

LLM 是 Large Language Model,大语言模型。

它是一类基于大量文本数据训练出来的深度学习模型,可以理解、生成和推理自然语言。

常见能力:

- 文本生成。
- 问答。
- 总结。
- 翻译。
- 代码生成。
- 信息抽取。
- 对话。
- 分类和推理。

面试回答可以说:

```text
LLM 是基于大规模语料训练的大语言模型,通过学习语言中的统计规律和上下文关系,可以完成问答、总结、代码生成、文本分类等任务。在工程应用中,我们通常会通过 Prompt、RAG、工具调用和评测体系来控制和验证模型输出质量。
```

## 2. LLM 和传统 NLP 模型有什么区别

传统 NLP 模型通常针对单一任务训练,例如分类、分词、命名实体识别。

LLM 的特点:

- 通用能力更强。
- 可以通过 prompt 适配不同任务。
- 具备一定上下文理解和推理能力。
- 可以少样本或零样本完成任务。
- 但也更容易出现幻觉、不稳定和不可解释问题。

## 3. 什么是 Token

Token 是模型处理文本的基本单位。

一个 token 可能是:

- 一个英文单词。
- 一个英文单词的一部分。
- 一个中文字符。
- 标点符号。

LLM 的输入和输出长度通常按 token 计算。

例如上下文窗口 8K tokens,表示模型一次最多能处理约 8K 个 token 的输入输出总长度。

## 4. 什么是 Context Window

Context Window 是模型一次能够处理的最大上下文长度。

如果输入内容超过上下文窗口,模型可能无法完整处理,需要:

- 文档切分 chunk。
- 检索相关片段。
- 摘要压缩。
- 分批处理。

RAG 的一个重要目的就是避免把全部知识都塞进 prompt,而是只检索相关上下文。

## 5. 什么是 Temperature

Temperature 控制模型输出的随机性。

- Temperature 越低,回答越稳定、保守、确定。
- Temperature 越高,回答越发散、有创造性,但也更容易不稳定。

测试和评测场景通常建议使用较低 temperature,例如 0 或 0.1,这样结果更可重复。

## 6. 什么是 Hallucination 幻觉

Hallucination 是指模型生成了看起来合理但事实错误、没有依据或上下文不支持的内容。

在 RAG 中,幻觉常见表现:

- 答案中出现检索上下文没有的信息。
- 编造不存在的配置、参数、命令。
- 把多个文档内容错误组合。
- 对不确定问题给出确定答案。

减少幻觉的方法:

- 使用 RAG 提供可靠上下文。
- Prompt 要求只基于上下文回答。
- 对无依据问题回答“不知道”。
- 使用 Faithfulness / Hallucination 指标评估。
- 对关键场景增加人工复核。

---

# 二、Prompt Engineering 基础知识

## 1. 什么是 Prompt

Prompt 是给 LLM 的输入指令,用于告诉模型任务、背景、约束和输出格式。

一个好的 prompt 通常包含:

- 角色:你是谁。
- 任务:要做什么。
- 上下文:参考信息是什么。
- 约束:不能做什么。
- 输出格式:用 JSON、表格、步骤还是自然语言。
- 示例:必要时给 few-shot example。

## 2. 什么是 System Prompt

System Prompt 是系统级指令,优先级通常高于用户输入。

常用于定义:

- 模型角色。
- 回答风格。
- 安全边界。
- 是否只能基于上下文回答。
- 输出格式。

RAG 中常见 system prompt:

```text
你是一个严谨的问答助手。只能基于提供的上下文回答问题。如果上下文中没有答案,请回答不知道,不要编造。
```

## 3. Zero-shot 和 Few-shot 是什么

### Zero-shot

不给示例,直接让模型完成任务。

例如:

```text
请判断下面文本的情感是正向还是负向。
```

### Few-shot

给几个示例,让模型模仿示例完成任务。

Few-shot 适合:

- 输出格式要求严格。
- 任务比较复杂。
- 模型容易误解任务。

## 4. Chain of Thought 是什么

Chain of Thought 是让模型逐步推理的一种提示方法。

但在真实产品中,不一定要把完整推理过程展示给用户。可以让模型内部分析后只输出结论和简短理由。

## 5. Prompt 设计常见问题

- 指令不清楚。
- 上下文太长。
- 输出格式不稳定。
- 没有约束模型不要编造。
- 没有说明回答不知道的条件。
- 没有给示例。

---

# 三、Embedding 与向量数据库

## 1. 什么是 Embedding

Embedding 是把文本转换成向量的过程。

文本向量可以表达语义相似度。

例如:

```text
“如何重启服务” 和 “服务怎么 restart”
```

虽然字面不同,但语义相近,向量距离会比较近。

## 2. 为什么 RAG 需要 Embedding

RAG 需要从知识库中找到和用户问题最相关的文档片段。

流程通常是:

```text
用户问题 → 转成 query embedding → 在向量数据库中搜索相似 chunk → 返回相关上下文
```

## 3. 什么是向量数据库

向量数据库用于存储和检索 embedding。

常见向量数据库:

- FAISS。
- Chroma。
- Milvus。
- Pinecone。
- Weaviate。
- Elasticsearch vector search。

## 4. 什么是相似度搜索

常见相似度计算:

- Cosine Similarity 余弦相似度。
- Dot Product 点积。
- Euclidean Distance 欧氏距离。

目标是找到和问题语义最接近的文档片段。

## 5. 什么是 Chunk

Chunk 是文档切分后的片段。

为什么要切分:

- 原始文档太长,不能全部塞进 prompt。
- 检索粒度太大容易带入无关内容。
- 检索粒度太小又可能丢失上下文。

常见策略:

- 按段落切分。
- 按标题切分。
- 固定 token 长度切分。
- 设置 overlap,避免上下文断裂。

---

# 四、RAG 基础知识

## 1. 什么是 RAG

RAG 是 Retrieval-Augmented Generation,检索增强生成。

它把信息检索和大模型生成结合起来。

基本流程:

```text
用户问题 → 检索相关文档 → 构造 Prompt → LLM 基于上下文回答
```

面试回答:

```text
RAG 是检索增强生成,核心思想是先从外部知识库检索与问题相关的上下文,再让大模型基于这些上下文生成答案。它可以减少模型幻觉,让答案更有依据,也方便接入企业内部知识而不需要重新训练模型。
```

## 2. RAG 为什么有用

LLM 自身有几个问题:

- 训练数据可能过时。
- 不知道企业内部私有知识。
- 容易幻觉。
- 无法直接追溯答案来源。

RAG 可以解决:

- 接入最新知识。
- 接入私有文档。
- 提供上下文依据。
- 降低幻觉。
- 支持引用来源。

## 3. RAG 的典型架构

```text
文档收集
  ↓
文档清洗
  ↓
文档切分 chunk
  ↓
生成 embedding
  ↓
写入向量数据库
  ↓
用户提问
  ↓
检索 top-k 相关 chunk
  ↓
构造 prompt
  ↓
LLM 生成答案
  ↓
答案评测和日志记录
```

## 4. RAG 中 top-k 是什么

top-k 表示检索时返回最相关的 k 个文档片段。

例如 top-k=3,表示返回 3 个最相关 chunk。

注意:

- k 太小,可能漏掉关键上下文。
- k 太大,可能带入噪音,让模型混淆。

## 5. RAG 常见问题

### 5.1 检索不到正确上下文

原因可能是:

- 文档没有入库。
- chunk 切分不合理。
- embedding 模型效果不好。
- query 表达和文档表达差异太大。
- top-k 太小。

### 5.2 检索到了上下文但回答错误

原因可能是:

- prompt 约束不够。
- 模型理解错误。
- 上下文太长。
- 上下文之间互相冲突。
- 模型没有遵守只基于上下文回答。

### 5.3 回答正确但格式不对

原因可能是:

- prompt 没有明确输出格式。
- 没有给示例。
- 没有做输出解析和校验。

## 6. 如何优化 RAG

可以从四个层面优化:

### 文档层

- 清洗无效内容。
- 保留标题和层级结构。
- 删除重复文档。
- 更新过期文档。

### 切分层

- 调整 chunk size。
- 增加 overlap。
- 按标题/语义切分。

### 检索层

- 调整 embedding model。
- 调整 top-k。
- 使用 hybrid search,结合关键词和向量检索。
- 使用 reranker 重新排序。

### 生成层

- 优化 prompt。
- 要求引用来源。
- 要求不知道就回答不知道。
- 限制输出格式。

---

# 五、LangChain 基础知识

## 1. 什么是 LangChain

LangChain 是一个用于构建 LLM 应用的开发框架。

它提供了一套组件,帮助开发者把 LLM、Prompt、Retriever、Memory、Tool、Agent、Vector Store 等组合起来。

面试回答:

```text
LangChain 是一个构建 LLM 应用的框架,它把 Prompt、模型调用、文档加载、文本切分、向量数据库、检索器、工具调用和 Agent 等能力模块化,方便开发 RAG、聊天机器人、自动化 Agent 等应用。
```

## 2. LangChain 主要解决什么问题

如果不用框架,开发 LLM 应用需要自己处理:

- 调模型 API。
- 管理 prompt。
- 加载文档。
- 切分文档。
- 生成 embedding。
- 连接向量数据库。
- 检索上下文。
- 管理对话历史。
- 调用工具。

LangChain 把这些能力封装成组件,提升开发效率。

## 3. LangChain 常见组件

### 3.1 LLM / ChatModel

用于调用大语言模型。

例如:

- OpenAI。
- Ollama。
- Azure OpenAI。
- Anthropic。
- 本地模型。

### 3.2 PromptTemplate

用于管理 prompt 模板。

例如:

```text
请基于以下上下文回答问题:
上下文: {context}
问题: {question}
```

### 3.3 Document Loader

用于加载不同格式文档。

例如:

- PDF。
- Markdown。
- TXT。
- HTML。
- Web page。

### 3.4 Text Splitter

用于把长文档切成 chunk。

### 3.5 Embeddings

用于把文本转成向量。

### 3.6 Vector Store

用于存储和检索向量。

例如 Chroma、FAISS、Milvus。

### 3.7 Retriever

用于根据 query 检索相关文档。

### 3.8 Chain

Chain 是把多个步骤串起来。

例如 RAG chain:

```text
question → retriever → prompt → llm → answer
```

### 3.9 Agent

Agent 可以根据任务自主决定调用哪些工具。

例如:

- 查知识库。
- 调 API。
- 执行脚本。
- 查询数据库。

## 4. Chain 和 Agent 的区别

Chain 是固定流程。

例如:

```text
先检索 → 再生成答案
```

Agent 是动态决策。

例如:

```text
用户问 CI 失败原因 → Agent 判断需要先查日志 → 再查历史问题 → 再总结原因
```

回答要点:

- Chain 更稳定、可控、适合固定流程。
- Agent 更灵活,适合需要多步决策和工具调用的任务。
- Agent 风险更高,需要更多约束和评测。

## 5. LangChain 的优点和缺点

优点:

- 快速搭建原型。
- 组件丰富。
- 生态好。
- 适合 RAG 和 Agent。

缺点:

- 抽象层较多,debug 有时不直观。
- 版本变化较快。
- 复杂场景可能需要自己封装。
- 生产系统要关注可观测性、错误处理和成本控制。

---

# 六、AI Agent 基础知识

## 1. 什么是 AI Agent

AI Agent 是能够基于目标进行思考、规划、调用工具并完成任务的智能体。

它不只是回答问题,还可以执行动作。

例如:

- 分析日志。
- 调用 Jenkins API。
- 查询知识库。
- 生成测试用例。
- 创建缺陷报告。
- 总结 PR 变更。

## 2. Agent 通常由哪些部分组成

- LLM:负责理解和推理。
- Tools:可调用工具,如搜索、数据库、API、脚本。
- Memory:保存历史上下文。
- Planner:任务拆解和计划。
- Executor:执行工具调用。
- Guardrails:安全约束。
- Evaluator:评估输出质量。

## 3. Agent 和普通 Chatbot 的区别

Chatbot 主要回答问题。

Agent 可以:

- 拆解任务。
- 决定下一步做什么。
- 调用工具。
- 根据工具结果继续推理。
- 完成端到端任务。

## 4. Agent 的风险

- 工具调用错误。
- 幻觉导致错误操作。
- 权限控制不足。
- 执行不可逆操作。
- 结果不可重复。
- 成本不可控。

所以工程中要做:

- 工具权限限制。
- dry-run。
- 人工确认。
- 日志记录。
- 输出评测。
- 安全边界。

---

# 七、DeepEval 与 LLM 应用评测

## 1. 什么是 DeepEval

DeepEval 是一个用于评测 LLM 应用的 Python 框架。

它类似于 LLM 应用的测试框架,可以把问题、模型回答、期望答案和检索上下文组织成测试用例,然后用不同指标评估输出质量。

面试回答:

```text
DeepEval 是一个 LLM 应用评测框架,可以把 LLM 输出当成测试对象,通过 Answer Relevancy、Faithfulness、Hallucination、Contextual Precision、GEval 等指标评估回答质量。它可以和 Pytest 集成,把 RAG/Agent 的质量评测自动化并接入 CI。
```

## 2. 为什么需要 LLM 评测

传统软件测试通常是确定性断言:

```text
actual == expected
```

但 LLM 输出是非确定性的,同一个问题可能有多种正确表达。

所以 LLM 评测需要关注:

- 是否回答了问题。
- 是否和期望答案语义一致。
- 是否基于上下文。
- 是否有幻觉。
- 是否格式正确。
- 是否安全。

## 3. 什么是 LLMTestCase

在 DeepEval 中,一个测试用例通常包含:

- input:用户问题。
- actual_output:模型实际回答。
- expected_output:期望答案。
- retrieval_context:检索到的上下文。
- context:参考上下文。

## 4. DeepEval 常见指标

### 4.1 Answer Relevancy

评估回答是否和用户问题相关。

如果用户问 A,模型答 B,相关性就低。

### 4.2 Faithfulness

评估回答是否忠实于给定上下文。

如果回答内容上下文中没有依据,Faithfulness 低。

### 4.3 Hallucination

评估回答是否包含幻觉。

如果模型编造不存在事实,Hallucination 风险高。

### 4.4 GEval

GEval 是一种通用评测方式,可以自定义 criteria。

例如:

- 正确性。
- 完整性。
- 简洁性。
- 是否符合格式。

### 4.5 Contextual Precision

评估检索到的上下文排序是否合理,相关内容是否排在前面。

### 4.6 Contextual Recall

评估检索上下文是否覆盖回答问题所需信息。

### 4.7 Contextual Relevancy

评估检索到的上下文是否和问题相关。

## 5. LLM-as-a-Judge 是什么

LLM-as-a-Judge 是用另一个 LLM 作为评委,根据标准对模型输出打分和解释原因。

优点:

- 能评估语义正确性。
- 比简单字符串匹配更灵活。
- 适合开放式回答。

风险:

- Judge model 也可能误判。
- 结果可能不稳定。
- 不同 judge model 分数可能不同。
- 对复杂专业问题可能判断不准。

改进方法:

- 使用清晰评测标准。
- 保留 judge reason。
- 设置低 temperature。
- 对关键问题人工复核。
- 使用固定 golden dataset 做回归。

## 6. Golden Dataset 是什么

Golden Dataset 是用于评测的标准测试集。

通常包含:

- 问题。
- 期望答案。
- 标准上下文或参考文档。
- 标签。
- 难度。
- 场景分类。

好的 golden dataset 应该:

- 来源真实。
- 覆盖核心场景。
- 包含边界和异常问题。
- expected answer 有依据。
- 可维护。
- 能区分模型/检索/数据问题。

## 7. RAG 评测应该评什么

RAG 评测分两部分:

### 7.1 检索质量

关注:

- 是否检索到正确文档。
- 正确文档是否排在前面。
- 是否召回足够上下文。
- 是否带入太多噪音。

指标:

- Contextual Precision。
- Contextual Recall。
- Contextual Relevancy。

### 7.2 生成质量

关注:

- 回答是否相关。
- 回答是否正确。
- 回答是否忠实上下文。
- 是否幻觉。
- 是否格式符合要求。

指标:

- Answer Relevancy。
- Faithfulness。
- Hallucination。
- Correctness / GEval。

## 8. RAG 失败如何定位

可以按链路拆解:

```text
问题 → 检索 → Prompt → 生成 → 评测
```

### 情况一:没有检索到正确上下文

重点看:

- 文档是否入库。
- chunk 是否合理。
- embedding 是否合适。
- query 是否需要改写。
- top-k 是否太小。

### 情况二:检索到了上下文但回答错

重点看:

- prompt 是否要求只基于上下文。
- 上下文是否太多或互相冲突。
- 模型能力是否不足。
- 是否需要更强模型或 reranker。

### 情况三:答案对但评测失败

重点看:

- expected answer 是否过窄。
- judge criteria 是否不清晰。
- judge model 是否误判。
- 是否需要人工复核。

---

# 八、AI 评测面试常问问题

## Q1: 什么是 LangChain?

**回答要点:**

LangChain 是构建 LLM 应用的框架,提供模型调用、PromptTemplate、Document Loader、Text Splitter、Embedding、Vector Store、Retriever、Chain、Agent 等组件,方便搭建 RAG、聊天机器人和工具调用型 Agent。

## Q2: 什么是 RAG?

**回答要点:**

RAG 是检索增强生成。它先从外部知识库检索与问题相关的上下文,再让 LLM 基于上下文生成答案。它可以减少幻觉、接入私有知识、提升答案可追踪性。

## Q3: RAG 的完整流程是什么?

**回答要点:**

```text
文档加载 → 文档清洗 → 文档切分 → 生成 embedding → 写入向量数据库 → 用户提问 → 检索 top-k chunk → 构造 prompt → LLM 生成答案 → 评测与日志记录
```

## Q4: 什么是 Embedding?

**回答要点:**

Embedding 是把文本转换成向量,让计算机可以用向量距离表示语义相似度。RAG 用 embedding 来做语义检索。

## Q5: 什么是向量数据库?

**回答要点:**

向量数据库用于存储和检索 embedding。用户问题被转成向量后,系统会在向量数据库中搜索最相似的文档片段。

## Q6: RAG 为什么能减少幻觉?

**回答要点:**

因为 RAG 给 LLM 提供了外部可靠上下文,并通过 prompt 约束模型只能基于上下文回答。如果上下文没有答案,应该回答不知道,从而减少凭空编造。

## Q7: RAG 一定不会幻觉吗?

**回答要点:**

不会。RAG 只能降低幻觉,不能完全消除。即使检索到了上下文,模型也可能理解错、引用错或编造。因此还需要 prompt 约束、评测指标、人工复核和日志追踪。

## Q8: RAG 中 chunk size 怎么选?

**回答要点:**

chunk 太大容易带入噪音,太小容易丢失上下文。一般要根据文档结构和任务调优,可以按标题/段落切分,设置 overlap,并通过检索评测观察效果。

## Q9: top-k 怎么选择?

**回答要点:**

top-k 太小可能漏召回,top-k 太大可能引入无关内容。需要结合 Contextual Recall、Relevancy 和最终回答质量调优。

## Q10: RAG 检索不到正确答案怎么办?

**回答要点:**

检查文档是否入库、chunk 切分是否合理、embedding 模型是否合适、query 是否需要改写、top-k 是否过小,必要时使用 hybrid search 或 reranker。

## Q11: 什么是 DeepEval?

**回答要点:**

DeepEval 是 LLM 应用评测框架,可以定义 LLMTestCase,使用 Answer Relevancy、Faithfulness、Hallucination、GEval 等指标评估模型回答,并可与 Pytest 集成接入 CI。

## Q12: 为什么 LLM 应用不能只用传统 assert 测试?

**回答要点:**

因为 LLM 输出不是完全确定的,同一个问题可能有多个正确表达。传统字符串匹配太死板,需要语义评估、上下文忠实性评估、幻觉检测和人工复核。

## Q13: 什么是 Answer Relevancy?

**回答要点:**

Answer Relevancy 衡量回答是否针对用户问题。如果用户问的是 RAG 流程,模型却回答 LangChain 定义,相关性就低。

## Q14: 什么是 Faithfulness?

**回答要点:**

Faithfulness 衡量回答是否忠实于给定上下文。如果答案中出现上下文没有支持的信息,忠实性就低。

## Q15: 什么是 Hallucination 指标?

**回答要点:**

Hallucination 指标用于检测模型是否生成了无依据或错误事实。RAG 中通常通过比较 answer 和 retrieval_context 来判断是否编造。

## Q16: 什么是 GEval?

**回答要点:**

GEval 是 DeepEval 中的通用评测指标,可以自定义评测标准,例如 correctness、completeness、format compliance,让 judge model 按标准打分。

## Q17: 什么是 LLM-as-a-Judge?

**回答要点:**

LLM-as-a-Judge 是用另一个 LLM 作为评委评估模型输出质量。它适合开放式回答评测,但也有误判和不稳定风险,所以要保留 reason 并结合人工复核。

## Q18: 怎么设计 Golden Dataset?

**回答要点:**

从真实用户问题和知识库出发,覆盖核心问题、高频问题、边界问题、负向问题和复杂问题。每个 expected answer 都要有文档依据,并标注场景和难度。

## Q19: 如果 DeepEval 分数低,你怎么定位?

**回答要点:**

先看失败的是哪个指标。如果 retrieval 指标低,检查文档、chunk、embedding、top-k。如果 faithfulness 低,检查 prompt 和上下文。如果 correctness 低,检查模型能力、expected answer 和 judge reason。

## Q20: 如何把 LLM 评测接入 CI?

**回答要点:**

把 golden dataset 变成 Pytest 参数化用例,每个问题调用 RAG/Agent 得到回答,再用 DeepEval 指标打分。低于 threshold 就 fail,同时输出 JSON/Markdown 报告用于分析。

## Q21: AI Agent 和 RAG 有什么区别?

**回答要点:**

RAG 主要解决基于知识库问答的问题,流程通常比较固定。Agent 更强调自主决策和工具调用,可以根据任务动态选择查知识库、调 API、执行脚本等操作。

## Q22: Chain 和 Agent 有什么区别?

**回答要点:**

Chain 是固定流程,更稳定可控。Agent 是动态决策,更灵活但风险更高,需要权限控制、日志、评测和人工确认。

## Q23: 什么是 Prompt Engineering?

**回答要点:**

Prompt Engineering 是设计和优化模型输入指令的方法,包括明确角色、任务、上下文、约束、输出格式和示例,让模型更稳定地完成任务。

## Q24: 如何减少 LLM 输出不稳定?

**回答要点:**

- 降低 temperature。
- 固定 prompt 模板。
- 使用结构化输出。
- 增加 few-shot 示例。
- 控制上下文质量。
- 使用评测和回归测试。

## Q25: 如果模型回答不知道,这是好事还是坏事?

**回答要点:**

要看场景。如果上下文确实没有答案,回答不知道是好事,说明模型没有编造。如果上下文有答案但模型不知道,说明检索或生成有问题。

## Q26: 如何评估一个 AI Agent 的质量?

**回答要点:**

- 任务完成率。
- 工具调用是否正确。
- 回答是否准确。
- 是否遵守权限和安全约束。
- 是否可解释。
- 成本和耗时。
- 异常场景是否可控。

## Q27: 企业内部做 RAG 要注意什么?

**回答要点:**

- 文档权限控制。
- 敏感信息保护。
- 文档更新机制。
- 检索效果评测。
- 答案引用来源。
- 幻觉检测。
- 日志和审计。

## Q28: RAG 和 fine-tuning 有什么区别?

**回答要点:**

RAG 是通过检索外部知识增强回答,适合知识频繁变化和私有知识问答。Fine-tuning 是调整模型参数,适合学习特定风格、格式或任务模式。知识更新类问题通常优先考虑 RAG。

## Q29: 什么时候需要 fine-tuning?

**回答要点:**

当模型需要稳定输出特定格式、特定风格、特定任务行为,并且 prompt/RAG 难以满足时可以考虑 fine-tuning。但如果只是补充知识,通常 RAG 更合适。

## Q30: 如何解释你做过的本地 RAG 评测框架?

**回答要点:**

```text
我用 Python、Pytest、DeepEval 和本地 LLM 服务搭了一个 RAG 评测框架。流程是先准备 golden dataset,每条数据包含问题和期望答案;测试时调用 RAG agent 生成回答,保存检索上下文和实际输出;然后用 DeepEval 的 Answer Relevancy、Faithfulness、Hallucination 和 Correctness 指标评测。最后生成 JSON 和 Markdown 报告,用于分析失败原因,比如是检索没召回、回答不忠实、还是 judge 误判。
```

---

# 九、面试时可以背的简短回答

## 1. LangChain 简短回答

```text
LangChain 是一个 LLM 应用开发框架,它把模型调用、Prompt、文档加载、文本切分、Embedding、向量数据库、Retriever、Chain 和 Agent 等能力组件化。用它可以比较快地搭建 RAG 问答、聊天机器人和工具调用型 Agent。不过生产环境中还需要关注可观测性、错误处理、版本变化和评测体系。
```

## 2. RAG 简短回答

```text
RAG 是检索增强生成,先从知识库检索和问题相关的上下文,再让大模型基于上下文回答。它适合接入企业内部知识和最新文档,可以降低幻觉并提供答案依据。一个典型 RAG 流程包括文档加载、切分、embedding、向量入库、query 检索、prompt 构造、LLM 生成和评测。
```

## 3. DeepEval 简短回答

```text
DeepEval 是 LLM 应用评测框架,可以像写自动化测试一样评估 LLM/RAG/Agent 的输出质量。它支持 Answer Relevancy、Faithfulness、Hallucination、GEval 等指标,可以和 Pytest 集成,把 golden dataset 变成自动化测试用例,并接入 CI 做质量回归。
```

## 4. AI 评测简短回答

```text
AI 评测不能只用传统字符串 assert,因为 LLM 输出具有不确定性。通常要从问题相关性、语义正确性、上下文忠实性、幻觉、格式、安全性等维度评估。对于 RAG,还要单独评估检索质量,比如上下文是否相关、是否召回关键信息、排序是否合理。
```

## 5. RAG 失败定位简短回答

```text
我会按链路定位:先看检索是否拿到正确上下文;如果没拿到,检查文档入库、chunk、embedding、top-k 和 query;如果拿到了但回答错,检查 prompt、上下文冲突和模型能力;如果答案看起来对但评测失败,再检查 expected answer 和 judge reason,必要时人工复核。
```
